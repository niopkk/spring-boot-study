<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.bbz.core.repository.SysUserRepository">

    <resultMap id="BaseResultMap" type="com.bbz.core.entity.UserEntity">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <id column="user_id" property="userId" jdbcType="VARCHAR"/>
        <result column="username" property="userName" jdbcType="VARCHAR"/>
        <result column="account" property="account" jdbcType="VARCHAR"/>
        <result column="password" property="passWord" jdbcType="VARCHAR"/>
        <result column="salt" property="salt" jdbcType="VARCHAR"/>
        <result column="email" property="email" jdbcType="VARCHAR"/>
        <result column="phone" property="phone" jdbcType="VARCHAR"/>
        <result column="sex" property="sex" jdbcType="INTEGER"/>
        <result column="age" property="age" jdbcType="INTEGER"/>
        <result column="status" property="status" jdbcType="INTEGER"/>
        <result column="create_time" property="createTime" jdbcType="BIGINT"/>
        <result column="update_time" property="updateTime" jdbcType="BIGINT"/>
        <result column="last_login_time" property="lastLoginTime" jdbcType="BIGINT"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_id, username,account, password, salt, email, phone, sex, age, status,
        create_time, update_time, last_login_time
    </sql>

    <select id="findUserList" resultMap="BaseResultMap">
          SELECT  id, user_id, username,account, password, salt, email, phone, sex, age, status,
                  create_time, update_time, last_login_time
          FROM sys_user WHERE ifnull(delflag,0)=0
    </select>

    <select id="findSysUserByAccountAndPassWord" resultMap="BaseResultMap">
        SELECT id, user_id, username,account, password, salt, email, phone, sex, age, status,
                  create_time, update_time, last_login_time
         FROM sys_user WHERE ifnull(delflag,0)=0 AND account=#{param1} AND password=#{param2}

    </select>

    <select id="findUserByAccount" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sys_user WHERE ifnull(delflag,0)=0 AND account=#{account}
    </select>

    <select id="findUserByUserId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sys_user WHERE ifnull(delflag,0)=0 AND user_id=#{userId}
    </select>

    <insert id="insertUser" useGeneratedKeys="true" parameterType="com.bbz.core.entity.UserEntity">
     INSERT INTO sys_user(user_id, username, account, password, email, phone, sex, age, status, create_time, delflag)
     VALUES (#{userId},#{userName},#{account},#{passWord},#{email},#{phone},#{sex},#{age},#{status},now(),0)
    </insert>

    <update id="updateUser" parameterType="com.bbz.core.entity.UserEntity">
        UPDATE sys_user SET username=#{userName},account=#{account},password=#{passWord},email=#{email},phone=#{phone},age=#{age},update_time=now() WHERE user_id=#{userId,jdbcType=BIGINT}
    </update>


    <delete id="deleteUserByUserId" parameterType="String">
        UPDATE sys_user SET delflag=1 WHERE user_id=#{userId}
    </delete>
</mapper>