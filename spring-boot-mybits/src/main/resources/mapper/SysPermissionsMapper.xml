<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bbz.dao.SysPermissionsDao">


    <resultMap id="resultSysPermissions" type="com.bbz.model.SysPermissions">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="url" property="url"/>
        <result column="pid" property="pid"/>
        <result column="create_time" property="createTime"/>
        <result column="modify_time" property="modifyTime"/>
    </resultMap>

    <sql id="sql">
       p.id,
       p.name,
       p.url,
       p.pid,
       p.create_time,
       p.modify_time
    </sql>

    <select id="findSysPermissionsByRoleId" resultMap="resultProduct">
        select
        <include refid="sql"></include>
        from sys_permissions p
        left join sys_role_permissions rp on p.id = rp.permissions_id
        where rp.role_id =#{roleId}
    </select>


</mapper>
